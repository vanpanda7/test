# T1 Energy - TOPCon Solar Technology Website

基于 Figma 设计图实现的响应式网站，使用 Tailwind CSS 构建，支持多断点响应式设计。

## 📋 目录

- [快速开始](#快速开始)
- [项目结构](#项目结构)
- [断点配置](#断点配置)
- [开发指南](#开发指南)
- [Tailwind CSS 说明](#tailwind-css-说明)

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 构建 Tailwind CSS

**开发模式**（自动监听文件变化）：
```bash
npm run build-css
```

**生产模式**（压缩输出）：
```bash
npm run build-css-prod
```

构建完成后，会在 `static/css/` 目录下生成 `tailwind.css` 文件。

### 3. 查看效果

在浏览器中打开 `index.html` 即可查看页面效果。

## 📁 项目结构

```
.
├── index.html                    # 主页面
├── package.json                  # npm 配置
├── tailwind.config.js            # Tailwind 配置（自定义断点）
├── src/
│   └── input.css                 # Tailwind 输入文件（自定义组件）
├── static/
│   ├── css/
│   │   └── tailwind.css         # 构建输出的 Tailwind CSS（运行 npm run build-css 后生成）
│   └── images/                  # 图片资源
│       ├── image-1.png          # Hero 背景图
│       ├── image-2.png          # Utility 产品图
│       ├── image-3.png          # Residential 产品图
│       └── logo.png             # Logo
└── TAILWIND_TO_CSS_GUIDE.md     # Tailwind CSS 转 CSS 对照说明文档
```

## 📱 断点配置

项目配置了 6 个自定义断点（在 `tailwind.config.js` 中）：

| 断点名称 | 最小宽度 | 说明 |
|---------|---------|------|
| `xs` | 393px | 小手机 |
| `sm` | 768px | 平板 |
| `md` | 1024px | 小桌面 |
| `lg` | 1440px | 桌面 |
| `xl` | 1920px | 大桌面 |
| `2xl` | 2560px | 超大桌面 |

## 💻 开发指南

### 页面结构

1. **Hero 区域** - 首屏大图展示，包含标题和 CTA 按钮
2. **标题区域** - "Two form factors. Powerful possibilities."
3. **产品展示区域** - Utility 和 Residential 两个产品卡片
4. **页脚区域** - 包含导航链接、社交媒体链接和版权信息

### 响应式设计

所有组件都使用 Tailwind CSS 的响应式工具类实现多断点适配：

```html
<!-- 响应式字体大小 -->
<h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl">
  标题
</h1>

<!-- 响应式间距 -->
<div class="px-4 sm:px-6 md:px-8 lg:px-12 xl:px-16">
  内容
</div>

<!-- 响应式布局 -->
<div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
  卡片
</div>
```

### 自定义组件

在 `src/input.css` 中定义了以下自定义组件：

- **`.btn-discover`** - Discover 按钮样式（带 hover 效果）

## 📖 Tailwind CSS 说明

如果你不熟悉 Tailwind CSS，请查看 **`TAILWIND_TO_CSS_GUIDE.md`** 文档。

该文档包含：
- Tailwind 类到 CSS 的完整对照表
- 响应式断点说明
- 常用组件样式对照
- 快速参考表

## 🎨 设计特点

- ✅ 完全响应式设计（6 个断点）
- ✅ 使用 Tailwind CSS 实现，代码简洁
- ✅ 结构清晰，易于维护
- ✅ 符合 Figma 设计图规范

## 🔧 技术栈

- **Tailwind CSS 3.4+** - 实用优先的 CSS 框架
- **HTML5** - 语义化标记
- **响应式设计** - 多断点适配

## 🌐 浏览器支持

- Chrome (最新)
- Firefox (最新)
- Safari (最新)
- Edge (最新)

## 📝 开发工作流

1. **启动开发模式**：
   ```bash
   npm run build-css
   ```
   这会启动监听模式，当你修改 HTML 或 Tailwind 配置时，CSS 会自动重新构建。

2. **编辑代码**：
   - 在 `index.html` 中使用 Tailwind 工具类
   - 在 `src/input.css` 中添加自定义组件
   - 在 `tailwind.config.js` 中修改配置

3. **查看效果**：
   - 刷新浏览器查看更改

4. **生产构建**：
   ```bash
   npm run build-css-prod
   ```

## ❓ 常见问题

**Q: 构建后样式没有生效？**  
A: 确保 `static/css/tailwind.css` 文件已生成，并且 `index.html` 中已正确引用。

**Q: 如何添加新的自定义样式？**  
A: 在 `src/input.css` 的 `@layer components` 或 `@layer utilities` 中添加。

**Q: 如何理解 Tailwind CSS 类？**  
A: 查看 `TAILWIND_TO_CSS_GUIDE.md` 文档，里面有详细的对照说明。

## 📄 许可证

MIT
